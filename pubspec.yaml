name: ntravel
description: Travel guide built with Flutter Framework.
publish_to: 'none' 
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  provider: ^6.0.1
  http: ^0.13.4
  
  flutter_localizations:
    sdk: flutter

  firebase_core: ^1.10.5
  firebase_database: ^9.0.3
  cloud_firestore: ^3.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/
    - lib/locales/
